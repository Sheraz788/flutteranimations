import 'package:flutter/material.dart';
import 'package:flutter_animations/widgets/app_toolbar.dart';

class AnimationPackages extends StatelessWidget {

  const AnimationPackages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppToolbar(title: "Animations Packages"),
      body: Container(),
    );
  }
}